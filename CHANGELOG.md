# Changelog

## Unreleased

## 1.9.2 - 2026-09-20

+ [修复] 普通一次性生图在已经取得并保存图片后，不再因仅缺少保留会话绑定身份而被误记为失败。
+ [调整] Web 工作台、异步图片任务和 OpenAI 兼容图片接口的默认模型统一为 `gpt-image-2.5`，旧版 `gpt-image-2` 仍可显式选择。
+ [调整] Codex 图片 2.5 工具模型默认改为质量优先的 `gpt-image-2.5-sunburst`，仍可在设置中切换为 `gpt-image-2.5-flare`。
+ [测试] 增加普通图片结果携带上游会话游标及保留会话缺失绑定状态的回归验证。

## 1.9.1 - 2026-09-18

+ [修复] 补齐图片成功返回、文本回复续轮询和兜底轮询中的会话清理参数，避免图片已下载保存后因参数缺失而被判定为调用失败。
+ [修复] 图片成功后的会话清理遵守保留会话标志，保护工作台连续编辑所需的上游会话。
+ [测试] 增加三个成功出图路径的回归验证，覆盖清理会话开关和保留会话标志的组合。

## 1.9.0 - 2026-09-17

+ [新增] 新增全屏图片编辑器，支持标注、擦除、套索、撤销、重做和蒙版局部编辑；可从历史对话、图片库或本地文件选择编辑源，并持久化选区和标注。
+ [新增] 新增 `gpt-image-2.5` / `codex-gpt-image-2.5` 生图模型适配，支持独立上游模型配置、Codex 工具模型切换以及 `xhigh` / `max` 质量档。
+ [新增] 后台新增管理员账单浏览，以及本项目、CPA、Sub2API 账号 JSON 导入能力。
+ [新增] Web 界面和后端错误目录新增中文、英文、越南文切换。
+ [新增] 日志管理支持按调用日志或账号日志分类清空，账号导入弹窗在小屏内独立滚动。
+ [修复] 图片任务使用独立会话并稳定绑定账号；断流后通过权威状态回读结果，避免文本游标和图片会话互相污染。
+ [修复] 上游请求统一接入代理运行时；补充英文内容拒绝识别、阶段化超时信息、失败会话 ID 和图片下载瞬时失败重试。
+ [修复] JSON 存储改为原子写入，降低写入中断导致数据文件损坏的风险。
+ [修复] 全屏编辑器补齐移动端工具栏、重做栈失效、选区恢复、图库重试、历史图片按需解码和可访问名称。
+ [调整] 所有版本检查、更新入口、部署文档和容器镜像均以 `zuelu/chatgpt2api` 为准。

## 1.8.1 - 2026-09-17

+ [调整] 项目更新源、GitHub 入口和 Docker 镜像切换至 `zuelu/chatgpt2api`。
+ [调整] 纳入当前生产环境使用的图片、会话、日志和安全兼容改动，作为后续维护基线。
+ [修复] 禁用生产环境不需要的自动账号刷新、备份、清理和 Turnstile 自动求解任务，并保留失败关闭策略。

## 1.8.0 - 2026-07-28

+ [新增] 新增默认请求上游模型名称和默认思考强度配置，支持在设置页面修改，并可通过模型名的 `-standard`、`-extended`、`-max` 后缀覆盖思考强度。
+ [修复] 新增没出图也移除本地对话的配置，支持在图片生成失败、超时或仅返回文本时异步隐藏对应对话记录。
+ [修复] `/v1/models` 汇总各账号类型的官方模型列表，文本请求按模型权限选择账号。
+ [修复] 过滤隐藏、非最终频道及发往内部工具的助手消息，避免搜索指令和推理内容泄漏到 API 输出。
+ [修复] 修复输出清洗误删代码和命令中标点前空格的问题。
+ [修复] 为图片生成 SSE 流增加可配置的硬超时上限，避免上游长连接长时间挂起。
+ [优化] 数据库存储改为增量同步，仅新增、更新或删除发生变化的记录，保留未变记录的 ID。

## 1.7.0 - 2026-07-05

+ [移除] 移除注册功能、防滥用机制导致封禁GitHub账号。

## 1.6.0 - 2026-07-04

+ [修复] 修复sub2api导入问题。
+ [修复] 修复前端404、405问题。
+ [新增] 新增出图后删除对话记录功能。
+ [调整] Pro号不再按无限额度处理、约每天1000张。

## 1.5.0 - 2026-06-13

+ [新增] 新增 WARP / Privoxy / FlareSolverr 清障方案，注册遇到 Cloudflare 拦截后可刷新 clearance 并重试。
+ [新增] 新增 `outlook_token` 邮箱池，支持 Outlook/Hotmail 注册验证码读取。
+ [新增] 新增网页搜索兼容接口、图片编辑 mask 和图片任务相关能力。
+ [优化] 更新 sentinel/PoW 获取方式，提高上游请求兼容性。
+ [优化] 调整代理优先级和注册请求重试逻辑。

## 1.4.1 - 2026-06-03

+ [新增] 账号刷新改为异步模式，支持前端轮询刷新/重新登录进度。
+ [新增] 号池管理页面新增重新登录功能，支持密码登录恢复异常账号。
+ [新增] 刷新后自动重新登录异常账号（可在设置页开启）。
+ [新增] 图片生成支持并行模式，多张图片使用独立线程和账号同时生成。
+ [新增] 图片轮询超时自动换账号重试（最多4次），连接超时同账号递增等待重试。
+ [新增] 图片二次确认机制与先check再hit可配置化，关闭后可跳过等待直接返回结果。
+ [新增] 图片任务进度追踪，显示当前生成步骤（上传/预热/获取token/生成中等）。
+ [新增] 图片超时后续轮询功能，前端显示"继续等待"按钮。
+ [新增] 设置页新增图片二次确认、超时等待时间、自动重新登录等配置项。
+ [优化] 优化生图页面滚动加载性能，图片懒加载、会话切换滚动位置保存与恢复。

## 1.4.0 - 2026-05-31

+ [新增] 新增AI生成可编辑PSD文件逆向。
+ [新增] 新增AI生成可编辑PPT文件逆向。

## 1.3.1 - 2026-05-30

+ [新增] 新增ChatGPT搜索调试、Skills。

## 1.3.0 - 2026-05-30

+ [新增] 新增ChatGPT搜索接口逆向。

## 1.2.4 - 2026-05-30

+ [新增] 添加聊天补全缓存与重复请求合并。
+ [新增] 新增无限画布一键跳转功能

## 1.2.3 - 2026-05-29

+ [新增] 新增账号级代理。
+ [修复] 修复503异常信息、前端邮箱换行问题。

## 1.2.2 - 2026-05-29

+ [新增] 新增Codex链路生图、支持2k,4k。
+ [新增] 支持RT刷新账号信息。

## 1.2.0 - 2026-05-28

+ [新增] 当前版本基线，包含 Web 面板、画图、号池管理、注册机、图片管理、日志管理和设置能力。
+ [新增] 前端版本号支持点击查看版本更新弹窗，展示当前版本、最新版本和更新日志。
+ [优化] 优化注册机效率，成功率大幅提高。
+ [优化] 优化生图页面配置选项。
